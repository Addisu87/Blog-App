<section class="container">
  <div class="user-box">
    <img class="image-user" src=<%= @user.photo %> alt="user"/>
    <div class="user-info">
      <p class="user-name"><%= @user.name %></p>
      <p class="user-post">Number of posts: <%= @user.posts_counter%></p>
    </div>
  </div>
  <p class="user-bio"><%= @user.bio %></p>
  <% @user.recent_posts.each do |post| %>
    <%= link_to user_post_path(@user.id, post.id) do %>
      <div class="user-recent-post">
        <p class="post-title"><strong>Post</strong> : <%= post.title %></p>
        <p class="post-text"><%= post.text %></p>
        <h4 class="counters">Comments: <%= post.comments_counter %>
          |  Likes: <%= post.likes_counter %>
        </h4>
      </div>
    <% end %>
  <% end %>
  <div class="d-grid gap-2 d-md-flex justify-content-md-start m-4">
    <%= link_to "See all posts", user_posts_url(@user), class: "btn btn-success" %>
  </div>
</section>
